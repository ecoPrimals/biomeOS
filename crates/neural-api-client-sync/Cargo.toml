[package]
name = "neural-api-client-sync"
description = "Synchronous Neural API client for biomeOS — zero-tokio, std + serde_json only"
version = "0.1.0"
edition.workspace = true
license = "AGPL-3.0-only"
repository = "https://github.com/ecoPrimals/biomeOS"

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
biomeos-test-utils = { path = "../biomeos-test-utils" }
serial_test = "3.2"
tempfile = "3.8"

[lints]
workspace = true
