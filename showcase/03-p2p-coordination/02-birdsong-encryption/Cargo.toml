[package]
name = "birdsong-encryption-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
biomeos-core = { path = "../../../crates/biomeos-core" }
biomeos-types = { path = "../../../crates/biomeos-types" }
anyhow = "1.0"
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1"

