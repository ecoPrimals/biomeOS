# Handoffs

Canonical handoffs live in `ecoPrimals/infra/wateringHole/handoffs/`.

This directory previously held local copies (v3.99–v4.04, wave 73–75).
Those have been archived to the canonical location and removed from this
repo to avoid stale duplicates.
